
\begin{table}[t]
\centering
\small
\caption{Operational cost quantities for the locked reliability-selection audit. Candidate counts are per model--dataset--seed split; five seeds multiply the full audit to 105 selector fits per condition. Activation storage is shown as a raw matrix calculation and, for the auxiliary tasks, as compressed NPZ file size observed in the uploaded validation package.}
\label{tab:operational-cost-quantification}
\begin{threeparttable}
\begin{tabularx}{\textwidth}{p{0.27\textwidth}X}
\toprule
Cost item & Quantification \\
\midrule
Cheap confidence/option selector & One forced-choice scoring pass; 3 logistic $C$ candidates per seed; no hidden-state storage. \\
PCA-hidden selector & Hidden-state extraction plus train-fitted scaler/PCA; $3$ PCA dimensions $\times$ $3$ logistic $C$ values = 9 candidates per seed. \\
Confidence/option + PCA-hidden selector & Cheap features plus hidden-state extraction and PCA; 9 candidates per seed. \\
Full locked audit grid & 21 selector candidates per seed (3 cheap + 9 hidden-only + 9 combined); 105 candidate fits per model--dataset condition over five seeds. \\
Raw activation storage & A hidden matrix with $n$ examples, dimension $d$, and $b$ bytes/value requires $ndb/2^{20}$ MiB. For 1000 AG News examples, Qwen2.5-7B with $d=3584$ requires 13.7 MiB in float32 or 6.8 MiB in float16; Llama/Mistral-style $d=4096$ requires 15.6 MiB in float32 or 7.8 MiB in float16. For 1400-example DBPedia files, the raw float32 range is 19.1--21.9 MiB. \\
Compressed auxiliary files & In the uploaded auxiliary extraction, compressed hidden NPZ files range from 1.7 MiB to 10.3 MiB per model--dataset condition; the maximum corresponds to a 1400-example DBPedia hidden matrix. \\
Deployment burden not captured by latency alone & White-box model access, activation extraction hooks, feature retention policy, PCA/probe retraining, target-distribution validation, calibration monitoring, and versioned maintenance. \\
\bottomrule
\end{tabularx}
\begin{tablenotes}[flushleft]\footnotesize\item The storage examples quantify raw activation volume and compressed file size for the uploaded auxiliary runs. They do not include upstream model weights, tokenizer cost, or serving infrastructure.\end{tablenotes}
\end{threeparttable}
\end{table}
